{
  "name": "linkattribution-web-sdk-dev",
  "version": "1.0.1",
  "description": "Link Attribution Web SDK for web applications",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsup",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["link", "attribution", "sdk"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "tsup": "^7.2.0"
  }
}